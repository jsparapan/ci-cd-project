_format_version: "3.0"

services:
  - name: api-principal
    url: http://127.0.0.1:3000
    routes:
      - name: rota-catch-all
        paths:
          - /
        strip_path: false

plugins:
  - name: rate-limiting
    config:
      minute: 1000
      policy: local